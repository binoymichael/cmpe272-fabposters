<?php
    // $ch = curl_init("http://localhost:8887/users/list");
    // $output = curl_exec($ch); 
    // curl_close($ch);
    // echo $output;
	// $json_decoded = json_decode($json_string, true);
	// foreach ($json_decoded as $record) {
	// 	echo $record['first_name'] . " " . $record['last_name'] . "<br/>";
	// }

	// // $json_decoded = json_decode($json_string, true);
	// // foreach ($json_decoded as $record) {
	// // 	echo $record['first_name'] . " " . $record['last_name'] . "<br/>";
	// // }
